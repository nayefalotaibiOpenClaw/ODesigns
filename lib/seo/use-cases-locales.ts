// Auto-generated translations for use cases — keyed by slug then locale
// Structure: { [slug]: { [locale]: UseCaseContent } }

import type { UseCaseContent } from "./use-cases";

type LocaleMap = Partial<Record<string, UseCaseContent>>;

export const useCaseLocales: Record<string, LocaleMap> = {
  // Translations will be added incrementally per locale
  // Each slug maps to { ar: {...}, es: {...}, pt: {...}, fr: {...}, tr: {...}, id: {...} }
};
